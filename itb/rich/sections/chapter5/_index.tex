\chapter{Discussion}
\label{ch:discussion}

This chapter demonstrates a source-code listing and discusses the results.

\section{Implementation Notes}
\label{sec:implementation}

% The listings package typesets source code. The visual style was set
% once in main.tex with \lstset. Pass language=... for syntax
% highlighting. Listing~\ref{lst:example} refers to it by label.
Listing~\ref{lst:example} shows a short code snippet.

\begin{lstlisting}[language=Python, caption={A short Python example.}, label={lst:example}]
def surrogate(x):
    """Return a prediction for input x."""
    return model.forward(x)
\end{lstlisting}

\section{Interpretation}
\label{sec:interpretation}

% A good discussion explains WHAT the results mean (in contrast to the
% Results chapter, which reports WHAT they are) and compares them with
% prior work, e.g. \citet{smith2020}.
\lipsum[8]

\section{Limitations}
\label{sec:limitations}
\lipsum[9]
